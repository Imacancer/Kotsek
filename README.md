## Getting Started

Build the project:

```bash
npm install
```

run the development server:

```bash
npm run dev
```

## when installing dependencies

- npm install library_name

## when building the app

- npm run build

## when adding a library

- npm install --save library_name --force

## when removing a library

- npm uninstall library_name

## when adding an shadcn/ui component

- npx shadcn@latest add component_name

## @ or / are the file routing symbols

## Oks na hahahah Design ka na
